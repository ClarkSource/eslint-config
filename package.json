{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "lerna run lint",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^3.11.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ClarkSource/eslint-config.git"
  }
}
