{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint:js": "eslint --ext ts,js .",
    "lint:rules": "lerna run lint:rules",
    "test": "lerna run test"
  },
  "devDependencies": {
    "eslint": "^6.8.0",
    "lerna": "^3.20.2",
    "typescript": "^3.8.3"
  },
  "engines": {
    "node": "^10.0 || ^12.0 || ^13.0 || ^14.0"
  },
  "volta": {
    "node": "12.16.1"
  }
}
