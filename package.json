{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "eslint --ext ts,js .",
    "test": "lerna run test"
  },
  "devDependencies": {
    "eslint": "^6.8.0",
    "lerna": "^3.20.2",
    "typescript": "^3.8.2"
  },
  "engines": {
    "node": "^10.0 || ^12.0 || ^13.0"
  }
}
